"""\nDay 20 Challenge: String Compression\n\nProblem:\nCompress a string by counting consecutive identical characters.\nExample: 'aabcccccaaa' -> 'a2b1c5a3'\nIf the compressed string is not shorter than original, return the original string.\n\nDifficulty: Easy\nApproach: Use iteration with counter\n"""\n\ndef compress_string(s: str) -> str:\n    """\n    Compress string using counts of repeated characters.\n    Time Complexity: O(n)\n    Space Complexity: O(n)\n    """\n    if not s:\n        return s\n    \n    result = []\n    count = 1\n    \n    for i in range(1, len(s)):\n        if s[i] == s[i - 1]:\n            count += 1\n        else:\n            result.append(s[i - 1] + str(count))\n            count = 1\n    \n    # Append the last group\n    result.append(s[-1] + str(count))\n    compressed = "".join(result)\n    \n    return compressed if len(compressed) < len(s) else s\n\n\ndef compress_string_optimized(s: str) -> str:\n    """\n    Optimized approach using itertools.groupby.\n    More Pythonic and concise.\n    """\n    from itertools import groupby\n    \n    if not s:\n        return s\n    \n    result = [key + str(len(list(group))) for key, group in groupby(s)]\n    compressed = "".join(result)\n    \n    return compressed if len(compressed) < len(s) else s\n\n\nif __name__ == "__main__":\n    # Test cases\n    test_cases = [\n        ("aabcccccaaa", "a2b1c5a3"),\n        ("abcdef", "abcdef"),  # No compression needed\n        ("aaaa", "a4"),\n        ("a", "a"),\n        ("", ""),\n        ("aabbcc", "a2b2c2"),\n        ("aaabbb", "a3b3"),\n    ]\n    \n    print("Testing compress_string (iteration approach):")\n    for input_str, expected in test_cases:\n        result = compress_string(input_str)\n        status = "✓" if result == expected else "✗"\n        print(f"{status} Input: '{input_str}' => Output: '{result}' (Expected: '{expected}')")\n    \n    print("\\nTesting compress_string_optimized (groupby approach):")\n    for input_str, expected in test_cases:\n        result = compress_string_optimized(input_str)\n        status = "✓" if result == expected else "✗"\n        print(f"{status} Input: '{input_str}' => Output: '{result}' (Expected: '{expected}')")
